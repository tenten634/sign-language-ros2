{
  "description": "Default primitive motion capabilities for a differential-drive mobile robot.",
  "actions": {
    "move_forward": {
      "description": "Move straight forward in a straight line.",
      "params": {
        "distance_m": {
          "type": "float",
          "min": 0.1,
          "max": 1.0,
          "step": 0.1,
          "description": "Distance to move forward in meters."
        }
      }
    },
    "move_backward": {
      "description": "Move straight backward in a straight line.",
      "params": {
        "distance_m": {
          "type": "float",
          "min": 0.1,
          "max": 1.0,
          "step": 0.1,
          "description": "Distance to move backward in meters."
        }
      }
    },
    "rotate_left": {
      "description": "Rotate in place counter-clockwise.",
      "params": {
        "angle_deg": {
          "type": "float",
          "min": 1.0,
          "max": 360.0,
          "step": 15.0,
          "description": "Rotation angle in degrees, positive for left turn."
        }
      }
    },
    "rotate_right": {
      "description": "Rotate in place clockwise.",
      "params": {
        "angle_deg": {
          "type": "float",
          "min": 1.0,
          "max": 360.0,
          "step": 15.0,
          "description": "Rotation angle in degrees, positive for right turn."
        }
      }
    },
    "wait": {
      "description": "Wait in place without moving.",
      "params": {
        "duration_s": {
          "type": "float",
          "min": 0.5,
          "max": 300.0,
          "step": 0.5,
          "description": "Waiting time in seconds."
        }
      }
    },
    "navigate_to": {
      "description": "Move to a named virtual location using odom-based motion.",
      "params": {
        "location": {
          "type": "string",
          "description": "Destination location ID from locations.json (e.g., kitchen, bedroom)."
        }
      }
    }
  }
}

